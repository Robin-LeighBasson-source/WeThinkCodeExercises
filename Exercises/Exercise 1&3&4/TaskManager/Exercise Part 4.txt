Exercise Part 4: Practical Application Findings
1. Planning: Feature Implementation
Business Rule: "Tasks that are overdue for more than 7 days should be automatically marked as abandoned unless they are marked as high priority."

A. Files to Modify:

models.js (To update the core domain model)

app.js (To add the business logic for the rule)

cli.js (To provide a way for the user to trigger this rule)

B. Outline of Changes:

Modify models.js: Add a new state to the TaskStatus enum to support this domain rule.

const TaskStatus = { ..., ABANDONED: 'abandoned' };

Modify app.js (TaskManager): Create a new method, such as cleanupAbandonedTasks().

Fetch all tasks from storage.

Filter for tasks where status !== TaskStatus.DONE and status !== TaskStatus.ABANDONED.

Check if isOverdue() is true AND the difference between the current date and dueDate is > 7 days.

Check if priority < TaskPriority.HIGH (meaning it is Low or Medium).

For tasks matching all criteria, update their status to ABANDONED and call this.storage.save().

Modify cli.js: Add a new command (e.g., node cli.js cleanup) to trigger the cleanupAbandonedTasks() method and output how many tasks were abandoned.

C. Questions to Ask the Team:

Trigger Mechanism: Should this cleanup process run automatically in the background every time the user runs any CLI command, or should it remain a manual maintenance command (like node cli.js cleanup)?

Priority Clarification: Does "high priority" mean strictly HIGH (priority 3), or does it include URGENT (priority 4) as well? (Assuming >= 3).

Domain Implications: If a task is marked as ABANDONED, should the dynamic scoring system in task_priority.js treat its score as zero, similar to a DONE task?

2. Reflection
How the AI prompts helped: The AI drastically accelerated my understanding of the codebase's Separation of Concerns (CLI vs. Business Logic vs. Storage). Instead of blindly reading files top-to-bottom, the AI helped me trace the data flow and separate the "intent" (user inputs) from the "context" (system rules), which made deciding where to put new features obvious.

Aspects still unsure about: I am still unsure about the specific testing implementations. While I know Jest is used, I don't yet know how the team mocks the file system (fs module) to safely test TaskStorage without overwriting the real tasks.json database.

Next steps to deepen understanding: My immediate next step is to run the npm test command and read through the tests/taskManager.test.js file. Examining the unit tests is the safest and fastest way to see exactly how edge cases (like date formatting and priority merging) are actually handled in practice.